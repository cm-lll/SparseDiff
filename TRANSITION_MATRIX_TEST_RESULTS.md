# 转移矩阵测试结果

## ✅ 所有测试通过

### 测试 1: 转移矩阵创建 ✅
- 成功创建 `HeterogeneousMarginalUniformTransition` 类
- 正确识别异质图模式（`heterogeneous=True`）
- 正确创建关系族特定的均匀分布矩阵
- 支持没有子类别的关系族（只有存在性，2 个状态）

### 测试 2: 转移矩阵计算 ✅

#### 单步转移矩阵 (get_Qt)
- 全局转移矩阵：形状正确 `(bs, X_classes, X_classes)`, `(bs, E_classes, E_classes)`
- 转移矩阵行和验证通过（每行和为 1）

#### 关系族特定的转移矩阵
- `affiliated_with`: 2 个状态 `(bs, 2, 2)` ✅
- `author_of`: 4 个状态 `(bs, 4, 4)` ✅
- `cites`: 2 个状态 `(bs, 2, 2)` ✅
- 所有关系族的转移矩阵行和验证通过

#### 累积转移矩阵 (get_Qt_bar)
- 形状正确，行和验证通过

#### 所有关系族的转移矩阵 (get_all_family_Qt_bar)
- 成功为所有关系族创建转移矩阵
- 形状和行和验证通过

### 测试 3: 同质图模式 ✅
- 同质图模式正确识别（`heterogeneous=False`）
- 使用全局转移矩阵（保留原逻辑）
- 即使指定 `edge_family_name`，也使用全局转移矩阵

## 关键特性

### 1. 支持没有子类别的关系族 ✅
- 对于只有粗边关系的关系族（如 `affiliated_with`, `cites`），只有 2 个状态：`[no-edge, exists]`
- 为了统一接口，仍然计算分布，但计算量很小（只有 2 个状态）

### 2. 同质图模式兼容 ✅
- 当 `edge_family_marginals=None` 时，自动切换到同质图模式
- 使用全局转移矩阵，完全保留原逻辑

### 3. 批处理支持 ✅
- 支持批次维度 `(bs, ...)`
- 所有转移矩阵正确广播

## 下一步

1. ✅ 转移矩阵类已测试通过
2. ⏳ 修改 `apply_sparse_noise` 方法，实现关系族隔离的边扩散
3. ⏳ 实现关系族隔离的非存在边采样（伯努利分布）

